% fig7_48.tex — 7-60: 旋转圆环 + 三根辐条 + 弹性绳 + 小球（俯视图）
\begin{tikzpicture}[font=\small,>=stealth]
  % 圆环
  \draw[thick] (0,0) circle (2.5);
  \node[above right] at (2.2,1.5) {$M$, $R$};
  % 圆心 O（竖直细轴）
  \fill (0,0) circle (2pt);
  \node[below left] at (0,0) {$O$};
  % 三根辐条（均匀分布，120°间隔）
  \draw[thick] (0,0) -- (2.5,0);
  \draw[thick] (0,0) -- ({2.5*cos(120)},{2.5*sin(120)});
  \draw[thick] (0,0) -- ({2.5*cos(240)},{2.5*sin(240)});
  % 弹性绳（从O沿辐条到小球）
  % 小球在角度θ₀处（< 120°）
  \def\ang{50}
  \draw[thick,blue!70] (0,0) -- ({1.25*cos(\ang)},{1.25*sin(\ang)});
  \node[font=\footnotesize,blue!70] at ({0.8*cos(\ang)},{0.8*sin(\ang)+0.2}) {弹性绳 $k$};
  % 小球 P
  \draw[thick,fill=red!40] ({2.5*cos(\ang)},{2.5*sin(\ang)}) circle (0.12);
  \node[above right] at ({2.5*cos(\ang)},{2.5*sin(\ang)}) {$m$};
  % θ₀ 标注
  \draw[->] (0.8,0) arc[start angle=0,end angle=\ang,radius=0.8];
  \node[right,font=\footnotesize] at ({0.6*cos(\ang/2)},{0.6*sin(\ang/2)}) {$\theta_0$};
  % 自由长度标注
  \node[font=\footnotesize,below] at (1.25,-0.2) {自由长度 $R$};
  % 旋转箭头
  \draw[->,thick] (0,2.8) arc[start angle=90,end angle=120,radius=2.8];
  \node[above] at (0,2.9) {$\omega$};
\end{tikzpicture}
