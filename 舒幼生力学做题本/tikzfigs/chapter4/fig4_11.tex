\begin{tikzpicture}[>=Stealth, thick]
    % 圆心 O 
    \coordinate (O) at (4, -2);
    
    % 画出表示天体的空心圆 (缩小半径)
    \draw (O) circle (0.8);
    
    % 圆心点及标号 O
    \fill (O) circle (1.5pt);
    \node[right=2pt] at (O) {$O$};
    
    % 半径 R 箭头
    \draw[->] (O) -- ++(-50:0.8) node[midway, below left=-2pt] {$R$};
    
    % 天体质量 M (放在圆内左上方)
    \node at (3.55, -1.45) {$M$};
    
    % 虚线：初始速度方向延长线 与 过圆心的水平辅助线
    \draw[dashed] (0, 0) -- (5, 0);
    \draw[dashed] (0, -2) -- (4, -2);
    
    % 碰撞参数 b
    \draw[<->] (0, -2) -- (0, 0) node[midway, fill=white] {$b$};
    
    % 质点及初速度 v0
    \coordinate (P) at (0, 0);
    \fill (P) circle (2pt);
    \draw[->] (P) -- (1.2, 0) node[midway, above] {$v_0$};
    
    % 引力偏转轨迹曲线 (重新计算端点以贴合缩小的圆面)
    \draw[decoration={markings, mark=at position 0.6 with {\arrow{>}}}, postaction={decorate}] 
        (P) .. controls (2.5, -0.1) and (3.0, -0.5) .. (3.35, -1.53); 
\end{tikzpicture}