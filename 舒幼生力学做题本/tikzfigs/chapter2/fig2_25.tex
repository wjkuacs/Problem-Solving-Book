\begin{tikzpicture}[font=\small]
  % 水平面
  \draw[thick] (-3,-0.5) -- (4,-0.5);

  % 圆板（俯视图略带透视）
  \draw[thick, fill=gray!15] (0,0) circle (1.8);

  % 中心 O
  \draw[fill=black] (0,0) circle (2pt);
  \node[above left] at (0,0) {$O$};

  % 旋转方向
  \draw[->, thick] (1.3,0) arc[start angle=0, end angle=270, radius=1.3];
  \node[below right] at (1.1,-0.8) {$\omega$};

  % O 点运动方向
  \draw[->, thick, blue] (0,0) -- (2.5,0);
  \node[above] at (1.25,0) {$\vec{v}$（$O$点运动方向）};

  % 合摩擦力方向
  \draw[->, thick, red] (0,-0.3) -- (-2.0,-0.3);
  \node[below] at (-1.0,-0.3) {$\vec{f}_{\text{合}}$（与 $\vec{v}$ 反向）};

  % 圆板上的点微元示意
  \foreach \angle in {30,60,...,330} {
    \coordinate (P\angle) at (\angle:1.2);
    \draw[thin, gray, ->] (P\angle) -- ++(\angle+90:0.3);
  }
  \node[below right] at (1.2,1.0) {各点线速度};
\end{tikzpicture}
